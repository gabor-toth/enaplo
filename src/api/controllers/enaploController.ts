/**
 * New typescript file
 *
 * @see http://www.cross-platform-blog.com/nodejs/typescript/nodejs-a-rest-web-api-expressjs-with-typescript/
 * @see http://mherman.org/blog/2016/11/05/developing-a-restful-api-with-node-and-typescript/#.WZylqChJZWE
 */

/*
app.get( '/api/sayhello/:name', ( request, response ) => {
  const name = request.params.name;

  if ( !isNaN( name ) ) {
    response
      .status( 400 )
      .send( 'No string as name' );
  } else {
    response.json( {
      'message': name
    } );
  }
} );
*/
